spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/emlt
    username: postgres
    #set your own postgreSQL password here
    password:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
server:
  shutdown: graceful
  port: '8080'